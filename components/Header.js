//import Link from 'next/link'
import AppBar from 'material-ui/AppBar';

const linkStyle = {
  marginRight: 15
}

const Header = () => (
    <div>
        <AppBar />
    </div>
)

export default Header;
